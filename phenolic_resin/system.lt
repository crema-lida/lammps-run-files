import "PHEN.lt"

write_once("Data Boundary") {
      0 60 xlo xhi
      0 60 ylo yhi
      0 50 zlo zhi
}

# phenol = new PHEN[3].move(12, 0, 0)
#                  [3].move(0, 12, 0)
#                  [3].move(0, 0, 12)
phenol = new PHEN[500]
